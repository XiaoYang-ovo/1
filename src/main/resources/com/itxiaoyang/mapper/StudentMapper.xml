<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itxiaoyang.mapper.StudentMapper">


    <update id="updateById">
        UPDATE student
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="no != null and no != ''">no = #{no},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="idCard != null and idCard != ''">id_card = #{idCard},</if>
            <if test="isCollege != null">is_college = #{isCollege},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="degree != null">degree = #{degree},</if>
            <if test="graduationDate != null">graduation_date = #{graduationDate},</if>
            <if test="clazzId != null">clazz_id = #{clazzId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>


    <delete id="deleteByIds">
        delete from student where id in
        <foreach item="id" collection="ids" separator="," open="(" close=")" index="">
            #{id}
        </foreach>
    </delete>


    <select id="list" resultType="com.itxiaoyang.pojo.Student">
        select s.*,c.name clazzName from student s left join clazz c on s.clazz_id = c.id
        <where>
            <if test="name != null and name != ''">
                s.name like concat('%',#{name},'%')
            </if>
            <if test="degree != null and degree != ''">
                and s.degree = #{degree}
            </if>
            <if test="clazzId != null">
                and s.clazz_id = #{clazzId}
            </if>
        </where>
    </select>


    <select id="countStudentClazzData" resultType="java.util.Map">
        select
        case when clazz_id=1 then 'JavaEE就业163期'
        when clazz_id=2 then '前端就业90期'
        when clazz_id=3 then 'JavaEE就业165期'
        when clazz_id=4 then 'JavaEE就业166期'
        when clazz_id=5 then '咨询大数据就业58期师'
        else '其他' end clazz,
        count(*) num
        from student group by clazz_id order by num;
    </select>


    <select id="countStudentDegreeData" resultType="java.util.Map">
        select
        case when degree=1 then '初中'
        when degree=2 then '高中'
        when degree=3 then '大专'
        when degree=4 then '本科'
        when degree=5 then '硕士'
        when degree=6 then '博士'
        else '其他' end name,
        count(*) value
        from student group by degree order by value;
    </select>


</mapper>